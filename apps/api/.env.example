# Postgres — matches docker-compose.yml (host port 5434)
DATABASE_URL=postgres://receiptly:receiptly@localhost:5434/receiptly

# OpenAI — used by the vision extraction (gpt-4o-mini)
OPENAI_API_KEY=sk-...

# API port (frontend expects 3001 by default; see apps/web/.env.example)
PORT=3001
